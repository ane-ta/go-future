* ADR-_: Метрики мониторинга
  :PROPERTIES:
  :ID:       adr-_
  :REFERS_TO_REQ: 
  :STATUS:   PROPOSED
  :DATE:     2026-04-14
  :END:

** Контекст
Система GoFuture переходит на микросервисную архитектуру с тремя географическими регионами (Asia, South America, Europe).

Требования к мониторингу:
- Обеспечить 99.99% доступности критических сервисов
- Сократить MTTR (среднее время восстановления) с 4+ часов до 15 минут
- Предоставить SRE и разработчикам информацию о состоянии системы
- Предоставить бизнес-аналитикам метрики для принятия решений

** Проблема
Выбрать метрики мониторинга для предотвращения и оперативной реакции на инциденты.

** Решение

*** Технические метрики:
| Метрика                   | Что измеряет               | Порог          | Действие                           |
| ------------------------- | -------------------------- | -------------- | ---------------------------------- |
| `event_lag`               | Задержка обработки события | > 5 сек        | Увеличить consumer'ов              |
| `consumer_lag`            | Накопление сообщений       | > 1000         | Увеличить партиции или consumer'ов |
| `dead_letter_queue_size`  | Необработанные события     | > 100          | Алерт инженерам                    |
| `event_processing_errors` | Ошибки обработки           | > 1% за минуту | Проверить схему данных             |
| `saga_duration`           | Длительность саги          | > 30 сек       | Оптимизировать шаги                |
| `saga_failures`           | Отменённые заказы          | > 5% за час    | Алерт, анализ причин               |
| `outbox_lag`              | Задержка CDC               | > 1 сек        | Проверить Debezium                 |
| `service_up`              | Доступность сервиса        | 0 (down)       | PagerDuty                          |

**Автоматические метрики (бесплатно для всех сервисов):**

| Метрика | Что измеряет | Тип |
|---------|--------------|-----|
| `process_cpu_seconds_total` | CPU время процесса (суммарно) | Counter |
| `process_resident_memory_bytes` | Память процесса (RSS) | Gauge |
| `process_virtual_memory_bytes` | Виртуальная память | Gauge |
| `process_open_fds` | Открытые файловые дескрипторы | Gauge |
| `process_max_fds` | Максимум открытых FD | Gauge |
| `python_gc_*` | Сборщик мусора (объекты, время) | Counter / Gauge |
| `python_info` | Версия Python и среда | Gauge (константа) |

*** Бизнес-метрики (добавляются в код):

| Сервис                   | Метрика                            | Тип       | Назначение                         |
| ------------------------ | ---------------------------------- | --------- | ---------------------------------- |
| **Booking Service**      | `bookings_created_total`           | Counter   | Количество созданных заказов       |
|                          | `bookings_cancelled_total`         | Counter   | Количество отменённых заказов      |
| **Orchestrator**         | `saga_duration_seconds`            | Histogram | Длительность саги (регион, статус) |
|                          | `saga_total`                       | Counter   | Количество саг (регион, статус)    |
|                          | `active_sagas`                     | Gauge     | Активные саги (регион)             |
| **Driver Service**       | `drivers_assigned_total`           | Counter   | Назначенные водители               |
|                          | `driver_search_duration_seconds`   | Histogram | Время поиска водителя              |
|                          | `driver_proposals_total`           | Counter   | Отправленные предложения           |
|                          | `driver_accepted_total`            | Counter   | Принятые предложения               |
|                          | `driver_rejected_total`            | Counter   | Отклонённые предложения            |
| **Payments Service**     | `payments_hold_total`              | Counter   | Холды платежей                     |
|                          | `payments_release_total`           | Counter   | Релизы платежей                    |
|                          | `payments_charge_total`            | Counter   | Финальные списания                 |
|                          | `payments_errors_total`            | Counter   | Ошибки платежей (по типу)          |
|                          | `payments_hold_duration_seconds`   | Histogram | Длительность холда                 |
| **Geo Service**          | `geo_search_total`                 | Counter   | Геозапросы                         |
|                          | `geo_search_duration_seconds`      | Histogram | Длительность поиска                |
|                          | `geo_errors_total`                 | Counter   | Ошибки Elasticsearch / Яндекс.Карт |
| **Fraud Service**        | `fraud_checks_total`               | Counter   | Проверки на фрод                   |
|                          | `fraud_alerts_total`               | Counter   | Обнаруженные фрод-инциденты        |
| **Notification Service** | `notifications_sent_total`         | Counter   | Отправленные уведомления (по типу) |
|                          | `notifications_errors_total`       | Counter   | Ошибки отправки                    |
| **Payouts Service**      | `payouts_completed_total`          | Counter   | Выплаты водителям                  |
|                          | `payouts_errors_total`             | Counter   | Ошибки выплат                      |
| **Analytics Service**    | `analytics_events_processed_total` | Counter   | Обработанные аналитические события |
|                          | `analytics_lag_seconds`            | Gauge     | Задержка обработки аналитики       |

**Как это работает:**
1. Каждый сервис подключает библиотеку =prometheus_client=
2. Сервис запускает HTTP-сервер (порт 8000) с эндпоинтом =/metrics=
3. Автоматические метрики появляются без дополнительного кода
4. Бизнес-метрики добавляются в код в ключевых местах
5. Grafana Alloy скрапит =/metrics= (pull) или сервис пушит через Alloy
6. Метрики доступны в PromQL для дашбордов и алертов в Grafana

** Ограничения и риски
| Риск | Вероятность | Смягчение |
|------|-------------|-----------|
| **Слишком много метрик** | Средняя | Использовать гистограммы с ограниченными bucket'ами, агрегировать на стороне Prometheus |
| **Высокая нагрузка на сервисы из-за сбора метрик** | Низкая | Метрики собираются в отдельном потоке, не блокируют основной |
| **Метрики не отражают реальную картину** | Средняя | Регулярный аудит метрик с бизнес-командой |
| **Хранение исторических метрик** | Средняя | Retention в Prometheus: 30 дней (технические), 90 дней (агрегированные) |
| **Разные версии метрик в разных сервисах** | Низкая | Стандартизация через общую библиотеку метрик |

